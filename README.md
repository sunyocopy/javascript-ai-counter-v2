🏗️ Rebar AI Counter (โปรแกรมนับจำนวนเหล็กด้วย AI)
โปรเจกต์นี้เป็น Web Application สำหรับนับจำนวนเหล็กเส้นก่อสร้าง (Rebar) แบบอัตโนมัติโดยใช้เทคโนโลยี AI (Deep Learning) ทำงานผ่านเว็บเบราว์เซอร์โดยตรง ไม่ต้องติดตั้งโปรแกรมลงเครื่อง

✨ ฟีเจอร์หลัก (Features)
AI Detection: ใช้โมเดล AI (.onnx) ในการตรวจจับและนับจำนวนเหล็กได้อย่างแม่นยำ

Browser-Based: ประมวลผลทุกอย่างบนหน้าเว็บ (Client-side) ด้วย JavaScript

Area Selection Mode: สามารถวาดเส้นรอบพื้นที่ที่ต้องการ (Polygon) เพื่อให้นับเฉพาะเหล็กที่อยู่ในกรอบที่กำหนดได้ (ดูจากโค้ดส่วน isInSelectionMode)

Visual Visualization: แสดงกรอบสี่เหลี่ยม (Bounding Box) รอบเหล็กที่ตรวจจับได้ พร้อมแยกสีสันชัดเจน

📂 โครงสร้างไฟล์ (File Structure)
index.html: หน้าเว็บหลักสำหรับอัปโหลดรูปและแสดงผลการนับ

rebar.onnx: ไฟล์โมเดล AI ที่ผ่านการเทรนมาแล้ว (ใช้สำหรับรันบนเว็บ)

rebar.pt: ไฟล์โมเดลต้นฉบับ (PyTorch) สำหรับการเทรนหรือแก้ไขเพิ่มเติม

export_model.py: สคริปต์ Python สำหรับแปลงโมเดลจาก .pt เป็น .onnx

.github/workflows: การตั้งค่า CI/CD สำหรับ GitHub Actions

🚀 วิธีการใช้งาน (How to Run)
เนื่องจากเป็นไฟล์ HTML และ JavaScript ที่มีการเรียกใช้ไฟล์โมเดลภายนอก คุณจำเป็นต้องรันผ่าน Local Server

Clone หรือดาวน์โหลดโปรเจกต์นี้ลงในเครื่อง

เปิดโปรเจกต์ด้วย VS Code

ติดตั้ง Extension ที่ชื่อว่า "Live Server" (ถ้ายังไม่มี)

คลิกขวาที่ไฟล์ index.html แล้วเลือก "Open with Live Server"

หน้าเว็บจะเด้งขึ้นมา สามารถอัปโหลดรูปภาพเหล็กเพื่อทดสอบการนับได้ทันที

🛠️ Tech Stack
Frontend: HTML5, JavaScript (Canvas API)

AI Inference: ONNX Runtime Web

Model: YOLO (Custom Trained for Rebar)

📝 Note สำหรับนักพัฒนา
หากต้องการแก้ไขโมเดล ให้เทรนใหม่และใช้ไฟล์ export_model.py เพื่อแปลงเป็น .onnx ก่อนนำมาวางทับไฟล์เดิม